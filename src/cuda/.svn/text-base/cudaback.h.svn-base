
#ifndef CUDABACK_H_
#define CUDABACK_H_

void back_estimate(
		int back_size,
		float backfthresh,
		float *backmean,
		float *backsig,
		int nbackfx,
		int nbackfy,
		int backtype);


void get_backresult(float *mean,
		float *sigma,
		float *backmean,
		float *backsig,
		float *dback,
		float *dsigma,
		int nb);

#endif
